# Vector Stores

The template supports multiple vector store backends.

## FAISS

FAISS is the default backend and persists vectors under `INDEX_DIR`.

## ChromaDB

ChromaDB is available through `VECTOR_STORE_PROVIDER=chroma`.
